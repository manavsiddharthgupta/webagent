const API_BASE = import.meta.env.WXT_API_BASE ?? "http://localhost:3000";

// TODO: streamChat() — POST /api/chat, read streaming response
// TODO: fillForm() — POST /api/fill, return structured JSON fills

export { API_BASE };
